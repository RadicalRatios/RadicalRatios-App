'use strict';

describe('TrukSpot. myTruck module', function() {

  beforeEach(module('TrukSpot.myTruck'));

  describe('my truck controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var myTruckCtrl = $controller('MyTruckCtrl');
      expect(myTruckCtrl).toBeDefined();
    }));

  });
});