'use strict';

describe('TrukSpot home module', function() {

  beforeEach(module('TrukSpot.home'));

  describe('Home controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var homeCtrl = $controller('HomeCtrl');
      expect(homeCtrl).toBeDefined();
    }));

  });
});